[install]
linker = "isolated" # "isolated" is the default, as of Bun v1.3+
# minimumReleaseAge = 259200 # 3 days in seconds
minimumReleaseAgeExcludes = []

## Disabling security scanner for now, as it frequently times-out
# [install.security]
# scanner = "@socketsecurity/bun-security-scanner"
