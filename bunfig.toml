[install]
linker = "isolated" # "isolated" is the default, as of Bun v1.3+
# TODO: add minimumReleaseAge back in
# minimumReleaseAge = 604800 # 7 days in seconds

# [install.security]
# scanner = "@socketsecurity/bun-security-scanner"
